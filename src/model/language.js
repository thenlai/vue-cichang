
export default class {

    constructor(name, id) {
        this.name = name
        this.id = id
        this.alphabet = []
    }

    setAlphabet() {
        let argc = arguments.length
        if(argc>0) {
            if(argc===2) {
                let begin = arguments[0]
                let end   = arguments[1]
            }
        }
    }

    randomLetters(word, alphbetLength) {
        let wordLength = word.length
    }
}