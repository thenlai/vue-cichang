class Game {}

class Selection extends Game {
    constructor(options, index) {
        super()
        this.options
        this.index
    }

    test(selection) {
        return this.index===selection
    }


}

export default class {
    constructor(
        spell,
        desc,
        selectDesc,
        selectWord,
    ) {

    }
}